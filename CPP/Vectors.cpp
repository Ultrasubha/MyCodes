#include <bits/stdc++.h>
using namespace std;

#define print(x) cout<<x<<endl;

int main() {
    int arr[4]={1,2,4,5};
    //for(int i=0; i<4; i++) print(arr[i]);
    //printf(*max_element(arr,arr+4));
    print(*max_element(arr, arr + 4));
    return 0;
}