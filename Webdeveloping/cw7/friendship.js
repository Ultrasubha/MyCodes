$scope.calculate=function()
{
$scope.letters=($scope.name1+$scope.name2).lowercase

$scope.sum=0;
for($scope.i=0;$scope.i<letters.length;$scope.i++)
{
$scope.sum=$scope.letters.charCodeAt[i];
}
$scope.sum+=$scope.sum % 101;